package main

import (
	"fmt"
)
//O for segue uma sintaxe básica onde:
//Primeiro, vem a instrução inicial: executada antes da primeira iteração
//Em seguida, a expressão de condição: avaliada antes de cada iteração
//E fechando, a pós-instrução: executado no final de cada iteração

func main() {
	for i := 1; i <= 10; i++ {
  fmt.Println("Valor da Variável de Iteração:", i) 
//Output: Sequencia do 1 ao 10.


//Neste caso, o laço funciona como o For i in range de Python, onde para cada i, até que a condiçaõ seja quebrada(), o for fará algo.

}
}
